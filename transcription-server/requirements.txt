# Comum
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=12.0
numpy

# Backend Mac (Apple Silicon)
# pip install mlx-whisper
mlx-whisper>=0.4.0; sys_platform == "darwin"

# Backend cross-platform (Linux/Windows/Mac sem MLX)
# pip install faster-whisper
faster-whisper>=1.0.0; sys_platform != "darwin"
